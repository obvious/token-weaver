<?xml version="1.0" encoding="UTF-8"?>

{{{header}}}
<resources>

  <style name="TextAppearance.{{class_name}}" parent=""/>

  {{#each text_styles}}
  <style name="TextAppearance.{{@root.class_name}}.{{textAppearanceName name}}">
    {{#each value}}
    {{{this}}}
    {{/each}}
  </style>
  {{/each}}

</resources>
