{
  "devDependencies": {
    "@types/node": "^14.11.2",
    "gts": "^3.1.1",
    "typescript": "^4.9.5"
  },
  "dependencies": {
    "style-dictionary": "^3.7.2",
    "token-transformer": "^0.0.30"
  },
  "scripts": {
    "lint": "gts lint",
    "clean": "gts clean",
    "compile": "tsc",
    "fix": "gts fix",
    "build": "npm run compile && node build/src/index.js"
  }
}
